#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	printf("All 2-digit positive integers.\n");
	for (int i = 10; i < 100; i++)
		printf("%d ", i);
	
	return 0;
}